pipeline{
  agent any
  stages {
    stage("build") {
      steps {
      echo 'building application in dev'
      }
    }
    stage("test") {
      steps {
        echo 'testing application in dev'
      }
    }
    stage("maintain") {
      steps {
        echo 'maintaining application in dev'
      dev
    }
  }
}
